import { DayDifferencePipe } from './day-difference-pipe';

describe('DayDifferencePipe', () => {
  it('create an instance', () => {
    const pipe = new DayDifferencePipe();
    expect(pipe).toBeTruthy();
  });
});
